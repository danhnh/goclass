language: go

go:
  - 1.12.x
  - master

before_script:
  - test-reporter before-build
script:
  - go test -coverprofile c.out ./ex3/...
after_script:
  - test-reporter after-build --coverage-input-type gocov --exit-code $TRAVIS_TEST_RESULT